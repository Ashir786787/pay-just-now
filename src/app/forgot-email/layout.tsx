import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = {
  title: "PayJustNow",
};

export default function ForgotEmailLayout({ children }: { children: ReactNode }) {
  return children;
}